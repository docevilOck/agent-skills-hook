# 图型分类

## 主图类型

| 类型 | 适用场景 | 输出要求 |
|------|----------|----------|
| 架构 / 边界图 | 需要说明系统长什么样、模块怎么分、边界在哪里 | 手写 ASCII 到 `.md` |
| 接入点图 | 需要说明这次改动从现有代码哪里接入 | 接入点要编号，并在图下配小表 |
| 流程图 | 需要说明一个功能从输入到输出怎么走 | 保留主路径，再补关键分支 |
| 前后对照图 | 需要说明改动前后行为有什么变化 | 尽量复用相同节点名，方便比较 |

## 补充图

只有在主图不够用的时候，才用下面这些图：

- 时序图：顺序关系重要时使用
- 状态图：生命周期变化重要时使用
- 类图：数据结构形状重要时使用

补充图不能替代主图，只能补细节。

## 选择顺序

如果一时拿不准，按这个顺序选：

1. 架构 / 边界图
2. 接入点图
3. 流程图
4. 前后对照图
5. 补充图
