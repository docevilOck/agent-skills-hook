# 结构体定义文档模板

```markdown
# [结构体名] 定义文档

## 1. 作用
[这个结构体是干什么的]

## 2. 分类
- 新增 / 修改 / 复用
- 所属模块：
- 对应 architecture 文档：

## 3. 生命周期与所有权
- 谁创建：
- 谁释放 / 复位：
- 生命周期：
- 是否跨中断 / 线程 / 回调边界：

## 4. 字段
| 字段 | 类型 | 作用 | 长期状态/临时值 | 写入方 | 读取方 | 备注 |
| --- | --- | --- | --- | --- | --- | --- |
| ... | ... | ... | ... | ... | ... | ... |

## 5. 使用方
- 创建方：
- 读取方：
- 修改方：
- 只读观察方：

## 6. 依赖关系
- 依赖的其他结构体：
- 关联的状态枚举 / 错误码：
- 关联的缓冲区 / 协议帧：

## 7. 设计约束
- 为什么放在这个结构体里：
- 为什么不放到全局 / 局部变量：
- 是否允许共享可变访问：

## 8. 变更说明
[如果是修改过的结构体，说明这次变化]
```
