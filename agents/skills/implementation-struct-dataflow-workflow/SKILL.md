---
name: implementation-struct-dataflow-workflow
description: 代码修改场景下，实现架构文档确认后，继续梳理结构体定义、数据流和流程图时使用
---

# 实现结构体与数据流工作流

## 作用

当实现架构文档已被确认，需要在编码前继续细化结构体、数据流、流程图等实现细节时使用。本 skill 仅用于**代码改动场景**。

它负责把架构文档进一步拆成三类产物：

- 结构体清单和结构体定义文档
- 数据流规划文档
- 各部分详细流程图文档

图怎么画、怎么命名、怎么渲染，统一遵循 [diagram-workflow](../diagram-workflow/SKILL.md)。

详细拆解文档默认落到 `docs/plans/YY-MM-DD_name/detail/<topic-name>.md`，如果有图源则同时保留 `docs/plans/YY-MM-DD_name/detail/<topic-name>.puml`。

默认优先处理嵌入式 C / 纯 C 场景里的结构体、缓冲区、协议帧、外设配置和数据流。

## 基本流程

1. 读取已确认的架构文档。
2. 提取本次改动涉及的结构体。
3. 区分新增、修改、复用的结构体。
4. 为结构体分组并写定义文档。
5. 为整体数据流写规划文档。
6. 为关键部分写流程图文档。
7. 如果内容过多，就继续拆文档，不要硬塞进一个文件。

## 输出边界

这个 skill 只负责细化实现细节文档，不负责产出执行计划，也不负责替用户确认这些细化结果已经通过审阅。

禁止在本阶段顺带产出以下内容：

- `writing-plans` 风格的执行步骤、实施路线、任务拆分、阶段安排
- “下一步开始编码”“可以直接进入实现”“已确认可以执行”这类未经用户审阅确认的结论
- 把结构体梳理、数据流梳理、流程图梳理直接写成待办清单或施工顺序

本阶段只允许产出三类内容：

- 结构体定义与结构体关系文档
- 数据流规划文档
- 流程图与流程说明文档

## 停止点

完成上述细化文档后，默认在这里停止，等待用户审阅。

- 不要自动进入 `writing-plans`
- 不要自动补“建议执行顺序”
- 不要把“文档已写完”表述成“方案已确认”

只有在用户明确要求进入下一步，或明确要求生成执行计划时，才进入 `writing-plans`。

## 文件组织

建议按职责拆成这些目录：

- `references/`：规则、模板、判断标准
- `structures/`：结构体定义文档
- `dataflow/`：数据流规划文档
- `flows/`：流程图文档

## 结果要求

- 结构体文档要能回答“干什么、谁在用、依赖什么”
- 数据流文档要能看清入口、转换、输出和边界
- 流程图文档要和结构体定义、数据流规划互相对得上

## 完成后

这一步完成后，先交给用户审阅。

只有用户明确确认这些细化文档通过审阅，或明确要求继续拆执行计划时，才进入 `writing-plans`。

## 生成后自检

如果出现以下任一情况，视为越界，必须删除或改写：

- 文档中出现执行计划、任务清单、阶段安排、建议路线
- 输出里直接声明“已确认”“可以开始实现”“进入下一步编码”
- 未经用户确认，就把当前结果衔接到 `writing-plans`

## 参考

- [结构体拆分规则](references/structure-splitting-rules.md)
- [结构体文档模板](references/structure-document-template.md)
- [数据流文档模板](references/dataflow-document-template.md)
- [流程图文档模板](references/flow-document-template.md)
